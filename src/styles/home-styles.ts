/* Variables */
import variables from './_variables';

const homeStyles = {
  link: {
    padding: '1rem',
    border: `1px solid ${variables.colorBlack}`,
    borderRadius: '.5rem',
    margin: '.5rem 1rem',
    textDecoration: 'none'
  }
};

export default homeStyles;
