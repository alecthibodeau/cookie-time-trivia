interface Theme {
  appFontFamily: {
    fontFamily: string;
  };
  header: {
    backgroundColor: string;
  };
  progressBarOuter: {
    borderColor: string;
  };
  progressBarInner: {
    backgroundColor: string;
  };
  buttonPrimary: {
    fontFamily: string;
    backgroundColor: string;
  };
  colorShade: string;
}

export default Theme;
